# Workflows

## Local Validation

```bash
./scripts/validate.sh
```

## Full Build

The full package build is defined in `.github/workflows/build.yml` and runs the
build runner after installing .NET MAUI workloads.
