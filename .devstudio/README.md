# DevStudio Notes

`shiny` is tracked as a library-package repository with a workload-heavy full build.
The local health gate is `scripts/validate.sh`; the authoritative full build remains
`.github/workflows/build.yml`.

The root `README.md` is a local maintainer entry point. The upstream project overview
is preserved in `readme.md`.
